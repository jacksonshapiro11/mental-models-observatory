# QUEUE — 2026-08-10 (Monday), in order

**Written 2026-08-09 22:4x ET. Dated filename on purpose: a dated name cannot hold a stale occupant
(WORK_ORDER_READBACK.md PART 12.8, law 2).** The chat is not the contract. This is the list a fresh
session should work from.

**Closed on 08-09, do not redo:** the standing rule + morning-changed re-reads (IMP-152), the audio
QA check (IMP-154), the register blacklist (IMP-155), the TTD record correction, and PART 12 of the
work order.

---

## 1. Three-replica audio arm

Run the audio read-back arm at **three replicas**, per the owner ruling of 2026-08-09, so the
audio-vs-markdown comparison has the same statistical footing as the markdown arm. The 2-replica run
on 08-09 is the pilot, not the measurement.

**Acceptance:** three reader transcripts per unit on the audio surface, rows in
`system/readback-ledger.json` carrying `product:"audio"` and a replica index, and the comparison
reported against the markdown arm's 3-of-3 on the same units. **Align by ENTITY, not by header** —
the GPT rewrite strips section announcements by instruction, so there are no headers to align on.

## 2. The 15 weekly read-back rows from 2026-08-08

Fifteen rows exist for `product:"weekly"`, week `2026-W32`, and nobody has read them. The weekly is
the product the owner listened to when he found both defects.

**Start here:** `markets-minute` is a **unanimous fail, cycle 1** — all three readers came back
"narrow breadth / exhausted rally" and none of them got what the unit meant. `lede` is 2-of-3
(logged, not actuated). `interesting-3` transmitted 3/3 with a drifting so-what.

**Acceptance:** a verdict per row, the unanimous fail either redrafted or written up as a residual
with a reason, and anything that is a substance defect rather than a transmission defect routed to
PART 7's claim-quality queue instead of being fixed as prose.

## 3. R2 — the batched recalibration event

**Both template changes land in ONE edit** (WORK_ORDER_READBACK.md PART 12.5): the entity flag and
the Question Test's third output. Then the new `TEMPLATE_HASH` recorded by amendment in 12.5, the
08-07 pair-3 calibration re-run against the new template at three replicas, and **the delta from the
80% / 94% baseline reported.** A move over five points on either arm goes to the owner before
anything gains authority.

## 4. `so_what` required and validated

PART 12.10 P1. `prepare` exits non-zero on a missing or under-four-word `so_what`, as
`UNGRADEABLE_SO_WHAT`. Selftest case plus the negative control (strip one `so_what` from a copy of a
live claims file, expect exit 1). Mechanical, no template change, lands independently of R2.

## 5. The calibration bar

PART 12.4. Both arms, three replicas, table reported in phase one's format. **Nothing gains authority
until it passes** — not the Question Test, not the challenger.

---

## Owed to the owner, not buildable without him

- **`content/daily-updates/2026-07-29-editor-log.md` is a tracked, committed, ZERO-BYTE file on the
  reader surface.** It went in with commit `c675ed2`. Deleting it is a deletion under
  `content/daily-updates/`, which the sacred rule reserves to him.
- **`_to_delete/tmp-dirtycheck-2026-08-09/`** — scratch from the 08-09 verification. `device_bash`
  cannot unlink files on his disk.
- **IMP-153, the archive header backfill** — `published-header-gate --all` fails pre-April and on
  some July files, live on the site. Batch-fix on a quiet day, with the full push protocol.
